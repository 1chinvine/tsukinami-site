<footer class="footer">
  <div class="footer-inner">

    <div class="footer-top">
      <a class="footer-logo" href="/">
        <img
          src="/images/logo/tsukinami-logo.png"
          alt="ツキナミ"
        />
      </a>

      <nav class="footer-nav">
        <a href="/">HOME</a>
        <a href="/news">NEWS</a>
        <a href="/schedule">SCHEDULE</a>
        <a href="/member">MEMBER</a>
        <a href="/discography">DISCOGRAPHY</a>
        <a href="/about">ABOUT</a>
      </nav>
    </div>

    <div class="footer-bottom">
      <p>UNOFFICIAL FAN SITE</p>
      <p>© TSUKINAMI UNOFFICIAL FAN SITE</p>
    </div>

  </div>
</footer>

<style>
  .footer {
    padding: 72px 40px 32px;
    border-top: 1px solid var(--line);
  }

  .footer-inner {
    max-width: 1200px;
    margin: 0 auto;
  }

  .footer-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 40px;
  }

  /* LOGO */

  .footer-logo {
    display: block;
  }

  .footer-logo img {
    display: block;
    width: auto;
    height: 32px;
  }

  /* NAVIGATION */

  .footer-nav {
    display: flex;
    flex-wrap: wrap;
    gap: 28px;
  }

  .footer-nav a {
    position: relative;

    color: var(--text);
    font-size: 12px;
    letter-spacing: 0.08em;
    text-decoration: none;
  }

  /* 流れ星ライン */

  .footer-nav a::after {
    content: "";

    position: absolute;
    left: 0;
    bottom: -8px;

    width: 100%;
    height: 1px;

    background: var(--accent);

    transform: scaleX(0);
    transform-origin: center;

    transition: transform 0.25s ease;
  }

  .footer-nav a:hover::after {
    transform: scaleX(1);
  }

  /* BOTTOM */

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;

    gap: 24px;

    margin-top: 80px;
    padding-top: 24px;

    border-top: 1px solid var(--line);

    color: var(--muted);
    font-size: 10px;
    letter-spacing: 0.1em;
  }

  .footer-bottom p {
    margin: 0;
  }

  /* MOBILE */

 @media (max-width: 700px) {
  .footer {
    padding: 20px 20px 14px;
  }

  .footer-top {
    flex-direction: column;
    gap: 14px;
  }

  .footer-logo img {
    height: 22px;
  }

  .footer-nav {
    width: 100%;

    display: flex;
    flex-direction: row;
    flex-wrap: wrap;

    gap: 8px 16px;
  }

  .footer-nav a {
    font-size: 9px;
    letter-spacing: 0.05em;
  }

  .footer-bottom {
    flex-direction: column;
    align-items: flex-start;

    gap: 4px;

    margin-top: 16px;
    padding-top: 10px;

    font-size: 8px;
  }
}
</style>